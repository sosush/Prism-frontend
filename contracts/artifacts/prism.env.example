# PRISM Registry deployment + integration config

# Chain RPC (Polygon Amoy default)
export WEB3_RPC_URL="https://rpc-amoy.polygon.technology"

# Deployed PRISMRegistry contract address
export WEB3_CONTRACT_ADDRESS="0x..."

# Java issuer (admin-paid minting) private key
# WARNING: do not commit this; keep it in secret manager/.env (ignored)
export WEB3_WALLET_PRIVATEKEY="..."

# EIP-712 issuer address (used when you switch to user-paid signature flow)
export PRISM_ISSUER="0x..."

# Attestation TTL (seconds)
export PRISM_ATTESTATION_TTLSECONDS="604800"

# Foundry deployment vars (if deploying from contracts/)
export PRISM_TTL="604800"
